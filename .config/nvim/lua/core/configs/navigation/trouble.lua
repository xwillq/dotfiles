return {
    -- Group results by file
    group = true,
    -- Add empty line at the top
    padding = false,
    -- Automatically open preview window for selected item
    -- TODO: remove error when opening preview for already opened file
    auto_preview = true,
    -- Automatically fold file list on creation
    auto_fold = false,

    use_diagnostic_signs = true,
}
