return {
    window = {
        border = 'single',
        -- Extra space around window [top, right, bottom, left]
        margin = { 0, 0, 1, 0 },
        -- Extra space inside of window [top, right, bottom, left]
        padding = { 0, 1, 0, 1 },
    },
    layout = {
        spacing = 3, -- Space between columns
        align = 'center', -- Column alignment
    },
    disable = {
        buftypes = {},
        filetypes = {
            'neotree',
        },
    },

    mappings = {
    },
}
